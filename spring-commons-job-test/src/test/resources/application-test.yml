spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    cn.yusiwen.spring: DEBUG
